# Governor private key -- must be the address that is governor on
# AeonVoter/FeeDistributor/EmissionsEngine (whichever set is currently live).
DEPLOYER_PK=

# Optional overrides
# RPC_URL=https://rpc.mainnet.chain.robinhood.com
# INTERVAL_MS=1800000

# After the AeonVoterV3 migration cuts over, update these three to the new
# addresses and restart -- no code change needed.
# VOTER_ADDRESS=
# FEE_DISTRIBUTOR_ADDRESS=
# EMISSIONS_ENGINE_ADDRESS=
